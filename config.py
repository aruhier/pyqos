#!/usr/bin/python

# INTERFACES
PUBLIC_IF = "eth0"  # network card which has the public IP
LAN_IF = "eth1"  # network card which has the "LAN" IP

# BANDWIDTH
UPLOAD = 5000  # upload in kbits/s
DOWNLOAD = 100000  # download in kbits/s

# If enabled, this script will not execute any command, just prints it
DEBUG = True
